# Riftbound Chase Cards

Single-page viewer for Riftbound (League of Legends TCG) cards, sorted by TCGplayer market price (chase cards first). Filters: set, min/max price, grid columns. Cards only — no sealed product.

Just open `index.html` in a browser (prices are fetched live from TCGplayer's public search API).
